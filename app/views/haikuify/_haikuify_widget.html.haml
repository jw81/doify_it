#haiku-widget
  %h2
    = 'Haikuify It'

  %button{ id: 'haikuify-it-btn', name: 'Haikuify Me', data: { target: '#haiku-text', path: haikuify_ajax_path } }
    = 'Haikuify Me'

  #haiku-text
    = render partial: 'haikuify_text'
